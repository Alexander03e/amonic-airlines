import { ReactElement } from "react";
import styles from './flight-control.module.scss'

export const FlightControl = (): ReactElement => {

    return (
        <div className={styles.wrapper}>

        </div>
    )
}