import { LabeledInput } from 'Common/components';

export const TicketsPage = () => {
    return (
        <div>
            <LabeledInput label='Номер бронирования' placeholder='Введите значение' />
        </div>
    );
};
